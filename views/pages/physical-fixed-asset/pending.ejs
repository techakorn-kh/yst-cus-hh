<%- include('../../layout/header.ejs'); %>

<%- include('../../layout/navbar.ejs'); %>

<div class="container py-3">
    <div class="row justify-content-center align-items-center">
        <h5 class="col-12 fw-semibold text-primary-emphasis" role="button" onclick="window.location.href='/physical-fixed-asset/list/<%= params?.physical_fa_no_id %>/<%= params?.company_id %>'">
            <label>
                <i class="fa-solid fa-angles-left"></i> <%= title %>
            </label>
        </h5>

        <!-- Detail - Pending -->
        <div class="col-12 py-3 mb-5">
            <%- details %>
        </div>

        <div class="col-12 fixed-bottom p-2 bg-light">
            <button class="btn btn-sm btn-light rounded-3 w-100 shadow text-primary-emphasis" onclick="window.location.href='/physical-fixed-asset/list/<%= params?.physical_fa_no_id %>/<%= params?.company_id %>'">
                <i class="fa-solid fa-backward"></i> Backward
            </button>
        </div>
    </div>
</div>

<%- include('../../layout/footer.ejs'); %>
